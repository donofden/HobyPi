# HobyPi Configuration
# Copy this file to .env and customize the values for your environment

# ===== Application Settings =====
APP_NAME="HobyPi Enhanced API"
APP_DEBUG=true

# ===== Database Configuration =====
# PostgreSQL connection URLs (async for app, sync for migrations)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost/hobypi
DATABASE_URL_SYNC=postgresql://postgres:postgres@localhost/hobypi

# Database connection components (used by scripts)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=hobypi

# ===== JWT Authentication =====
# IMPORTANT: Change JWT_SECRET in production!
JWT_SECRET=your-super-secret-jwt-key-change-in-production-please
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
JWT_ISSUER=hobypi
JWT_AUDIENCE=hobypi-clients

# ===== Bootstrap Admin User =====
# Default admin user created automatically on first startup
BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_EMAIL=admin@local
BOOTSTRAP_ADMIN_NAME=HobyPi Administrator
BOOTSTRAP_ADMIN_PASSWORD=letmein

# ===== Development Settings =====
# Port configuration for development servers
REACT_PORT=3000
API_PORT=8000

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://192.168.1.0/24

# ===== Security Notes =====
# For production deployment:
# 1. Generate a strong JWT_SECRET: openssl rand -hex 32
# 2. Change default admin password
# 3. Use environment-specific database credentials
# 4. Enable HTTPS and proper CORS settings
# 5. Set up proper PostgreSQL authentication
